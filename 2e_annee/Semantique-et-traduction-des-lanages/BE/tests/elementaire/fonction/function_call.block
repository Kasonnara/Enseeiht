function_call {
    // Test de function_call (template auto-générée)
    <int,character> f_empty(character x, int y){
                        int a = 42+y;
                        <int,character> b = <a,x>;
                        return b;
             }
    <int, character> res = f_empty('<',8);
}