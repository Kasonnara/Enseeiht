record_elt_access {
    // Test de record_elt_access (template auto-générée)
    typedef struct t_record { character x; int y; <int,int> z; } basic_struct;

    basic_struct a_record = {'#', 5, <42,13>};
    print a_record.y;
}