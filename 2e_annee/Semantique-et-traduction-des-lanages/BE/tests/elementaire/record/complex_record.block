complex_record {
    // Test de complex_record (template auto-générée)
    typedef struct t_record { character x; int y; <int,int> z; } basic_struct;

        basic_struct *a_record = new basic_struct();
}